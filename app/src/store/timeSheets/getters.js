const timeSheets = state => state.timeSheets;
const userId = state => state.userId;
const pickerDate = state => state.pickerDate;
const showType = state => state.showType;
const range = state => state.range;

export default {
    timeSheets,
    userId,
    pickerDate,
    showType,
    range
};
